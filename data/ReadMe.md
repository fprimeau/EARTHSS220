# EARTHSS 220 Data

Save your data in this directory, which is ignored by git (via the `.gitignore` file), so that the data you create will *not* be uploaded (or downloaded) if you push (or pull) this repository.